测试地址http://124.42.110.87:8181/ecc/

一、登录部分
1.版本号8.1.3,凡出现版本地方必须全局统一
2.已经登录的用户,再输入http://124.42.110.87:8181/ecc/ 应该直接进入主界面
3.同一个用户名可以登录多个浏览器画面,相互不冲突

二、总体布局
1.题头部分：题头左边点Logo跳转到公司网站,题头中间部分，显示当前操作员姓名，在线总人，检测器数量信息,题头右边部分，注销可退出到登录界面
2.主体部分：主体左边功能树，主体中间信息面板，右边操作面板。
3.中间面板：显示／隐藏整体树，显示／隐藏操作树，被记录到浏览器，下次登录后自动应用上次的设置状态。

三、监控器页签
1.　树节点分监测器节点和功能菜单，逐级展开，初始化自动选择第一个SE节点。
2. 树上设备定位功能，视图刷新功能
3. 节点状态，监测器状态从svdb获得，其他节点状态根据子节点状态统计。
4. 节点图标采用类别和状态组合来决定，不同类别和状态，提供不同图标，以示区别。
	
5. 树节点分右键菜单，根据授权数据自动显示有权限项目。
6. 列表行右键菜单，根据授权数据自动显示有权限项目。
7. 列表标题栏下拉菜单，根据授权数据自动显示有权限项目。
8. 树上当前选择节点，在中间信息面板的标题和下拉菜单，根据授权数据自动显示有权限项目。
	
9. 树上当前选择节点，还有的设备，监测器，正常、错误、危险、禁止数量显示。
	
10. 树上当前选择节点，在中间面板显示其下级子节点信息，显示方式分列表视图、图标视图、树状视图。
11.树上当前选择节点，在中间面板显示其下级子节点信息，显示数据分显示缩略、显示细节。
　　显示缩略只显示其儿子的节点信息，显示细节要显示其子孙所有节点的信息。
12.树上当前选择节点，在中间面板显示其下级子节点信息，根据状态提供筛选，显示全部，显示错误和危险、......
	
13.树上选择当前节点，树节点操作面板自动刷新其操作菜单，并将其第一个子节点操作菜单显示在“列表行操作”，没有子节点则隐藏列表行操作。
14.点中间面板列表行，操作面板自动刷新其操作菜单。

四、列表视图
1.支持按列排序，数据大时支持分页显示 
2.支持标题点击展开下级，如果是监测器，则点击后显示属性面板。　属性面板具体内容由bin.liu提供。
3.状态图标，鼠标移动上去，显示摘要信息，图标放大。
4.列表按照节点类型分组，分SE，组，设备，监测器，每组显示与否根据当前选择树［节点类型］＋［显示缩略／显示细节］决定。
5.标题后带下拉菜单按钮，下拉显示其操作菜单。

五、图标视图
1.状态图标，鼠标移动上去，显示摘要信息，图标放大。
2.支持标题点击展开下级，如果是监测器，则点击后显示属性面板。　属性面板具体内容由bin.liu提供。
3.标题后带下拉菜单按钮，下拉显示其操作菜单。
4.图标视图按照节点类型分组，分SE，组，设备，监测器，每组显示与否根据当前选择树［节点类型］＋［显示缩略／显示细节］决定。
5.支持标题单击展开下级，如果是监测器，则点击后显示属性面板。　属性面板具体内容由bin.liu提供。
6.支持状态图标双击展开下级，如果是监测器，则点击后显示属性面板。　属性面板具体内容由bin.liu提供。

六、树状视图
1.对于当前选择的整体树节点，将其父节点作为开始，自动全展开，到设备一级。
2.每个设备上显示其拥有的全部监测器。
3.每个监测器移动鼠标上去，显示其摘要信息。
4.每个监测器支持右键出现操作菜单。
5.双击监测器，会自动跳转到该设备的列表视图。

七、显示缩略和显示细节
1.对于列表视图，当前选择的整体树节点，显示缩略就是只显示其下一级的节点信息；显示细节则显示所有子孙的节点信息。
2.对于图标视图，当前选择的整体树节点，显示缩略就是只显示其下一级的节点信息；显示细节则显示所有子孙的节点信息。
3.对于树状视图，当前选择的整体树节点，显示缩略就是隐藏掉没有监测器的组和设备；显示细节则全部树节点都要显示。

八、自动刷新界面
1.可调节刷新时间，最快１秒，最慢100秒刷新。
2.设置的刷新时间，自动记录到浏览器，下次登录会应用上次设置的时间。
3.自动刷新会将其他用户维护的节点显示到自己界面上。

九、消息队列
1.系统消息实现了不同用户的操作信息之间互相告之。
2.目前有增，删，改事件，上下线事件，授权变化事件，将来还会补充更多事件。
3.增，删，改事件分两种，一是bin.liu在增删改完成后触发，另一个是jiantang.chen通过比对svdb中数据，发现的数据变化。
4.上线和下线，触发的事件，可以通过在线信息页签查看。
5.授权数据变化，通过消息机制可以立刻体现到被授权的用户身上，如果被授权人正在进行界面操作，其可见树节点，操作菜单会自动刷新。

十、在线信息页签
1.显示服务器信息
2.显示登录下线信息

十一、用户授权界面
1.对于admin用户，拥有所有权限
2.授权树上的复选框，只决定树节点是否可见。
3.授权树上当前选择节点，显示在右边操作面板上方，右边操作面板完成对该节点的授权。
4.操作授权，会体现到“授权树上当前选择节点”的所有下级节点上，直到其子节点被单独授权为止。

十二、嵌入式分组按钮
1.列表视图、图标视图、树状视图为一组
　默认为列表视图被点下。
　按下其中一个会弹起另一个，浏览器记录状态，下次登录应用该状态。

2.显示缩略、显示细节为一组。
　默认为显示缩略被点下。
　按下其中一个会弹起另一个，浏览器不记录状态，登录总显示缩略方式，避免登录时间太长。

3.显示/隐藏整体树，显示／隐藏操作面板相互独立。
　默认都被点下。
　按下后可以点击弹起，互相不影响。
　